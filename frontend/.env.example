# React reads variables prefixed with REACT_APP_
# Rename this file to .env before running the app

# Backend API base URL (update to your Render URL when deployed)
REACT_APP_API_URL=http://localhost:5000/api
